$(".portfolio").slick({
    autoplay: true,
    autoplaySpeed: 4000,
    pauseOnHover: false
});

    /* 點圖放大 */
    $(".list").fancybox({
        closeBtn		: false,
        helpers	: { buttons	: {}  }
          
     });
 